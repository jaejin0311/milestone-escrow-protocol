# RPC URL (Anvil local or Sepolia); required for API routes
ESCROW_RPC_URL=http://127.0.0.1:8545
# Optional: same value for client-side tx wait (or leave unset for default public RPC)
# NEXT_PUBLIC_ESCROW_RPC_URL=
# Factory contract address (from contracts deploy)
FACTORY_ADDRESS=0x

# Demo signer keys (never commit real keys; 0x prefix required)
CLIENT_PK=0x
# Optional: omit or set same as CLIENT_PK to use one wallet for both client and provider (testing)
PROVIDER_PK=0x

# Supabase (required for escrow list + proof storage)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
